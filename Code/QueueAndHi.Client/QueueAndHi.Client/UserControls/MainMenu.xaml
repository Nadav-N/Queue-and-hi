<UserControl x:Class="QueueAndHi.Client.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="90" d:DesignWidth="600">
    <StackPanel Orientation="Horizontal">
        <StackPanel.Resources>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="10,0,10,0"/>
                <Setter Property="Background" Value="Gray"/>
                <Setter Property="TextBlock.Foreground" Value="White"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Padding" Value="5,0,5,0"/>
            </Style>
        </StackPanel.Resources>
        <Image Height="90" Width="160" Source="..\Resources\virtual-queue.png"/>
        <TextBlock FontSize="20" VerticalAlignment="Center" FontWeight="Bold" Margin="40,0,470,0">
            <TextBlock.Effect>
                <DropShadowEffect ShadowDepth="1"/>
            </TextBlock.Effect>
            <TextBlock.Text>Queue &amp; Hi</TextBlock.Text>
        </TextBlock>
        <Button>Questions</Button>
        <Button>My Unanswered</Button>
        <Button>Ask Question</Button>
    </StackPanel>
</UserControl>
